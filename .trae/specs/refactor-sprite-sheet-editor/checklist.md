- [x] `src/main.tsx` 的职责已明显收敛，不再集中承载 3000+ 行业务、算法和长篇 JSX
- [x] 共享类型已拆分到独立模块，并被相关代码复用
- [x] 配置常量已拆分到独立模块，并被相关代码复用
- [x] 背景处理、像素清理、像素规范化、切帧、导出等纯函数已拆分到独立工具模块
- [x] 主要 UI 区域已拆分为语义明确的小组件，组件 props 清晰
- [x] 重构后导入、去背景、背景预览、橡皮擦、像素清理、像素规范化、切帧、重组、动画预览和导出功能保持可用
- [x] 重构没有引入新的 TypeScript 诊断错误
- [x] `npm run build` 构建成功
